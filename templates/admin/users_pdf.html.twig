<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Liste des Utilisateurs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 20px;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .date {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .no-data {
            text-align: center;
            color: #666;
            font-style: italic;
        }
        .badge {
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 10px;
            font-weight: bold;
            color: white;
        }
        .badge-admin { background: #dc3545; }
        .badge-coach { background: #ffc107; color: #000; }
        .badge-specialiste { background: #28a745; }
        .badge-user { background: #6c757d; }
    </style>
</head>
<body>
    <h1>Liste des Utilisateurs</h1>
    <div class="date">Généré le: {{ date|date('d/m/Y H:i:s') }}</div>
    
    {% if users|length > 0 %}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom complet</th>
                    <th>Email</th>
                    <th>Date d'inscription</th>
                    <th>Score Global</th>
                    <th>Rôle</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.id }}</td>
                        <td>{{ user.nomComplet }}</td>
                        <td>{{ user.email }}</td>
                        <td>{{ user.dateInscription ? user.dateInscription|date('d/m/Y') : '-' }}</td>
                        <td>{{ user.scoreGlobal ?? 0 }}</td>
                        <td>
                            {% if 'ROLE_ADMIN' in user.roles %}
                                <span class="badge badge-admin">Admin</span>
                            {% elseif 'ROLE_COACH' in user.roles %}
                                <span class="badge badge-coach">Coach</span>
                            {% elseif 'ROLE_SPECIALISTE' in user.roles %}
                                <span class="badge badge-specialiste">Spécialiste</span>
                            {% else %}
                                <span class="badge badge-user">Utilisateur</span>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <div style="margin-top: 30px; text-align: center; color: #666;">
            <strong>Total:</strong> {{ users|length }} utilisateur(s)
        </div>
    {% else %}
        <div class="no-data">Aucun utilisateur trouvé.</div>
    {% endif %}
</body>
</html>
