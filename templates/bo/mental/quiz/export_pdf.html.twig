<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Export Quiz Mental - BioSync</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            line-height: 1.4;
            color: #333;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #667eea;
            padding-bottom: 20px;
        }
        .header h1 {
            color: #667eea;
            font-size: 24px;
            margin: 0;
        }
        .header p {
            color: #666;
            font-size: 14px;
            margin: 5px 0 0 0;
        }
        .quiz-item {
            margin-bottom: 25px;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            background: #f9f9f9;
        }
        .quiz-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
        }
        .quiz-title {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            flex: 1;
        }
        .quiz-meta {
            display: flex;
            gap: 20px;
            font-size: 12px;
            color: #666;
        }
        .quiz-info {
            margin-bottom: 10px;
        }
        .quiz-info span {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 11px;
            margin-right: 10px;
        }
        .user-info {
            background: #e8f4fd;
            padding: 10px;
            border-radius: 6px;
            margin-bottom: 10px;
        }
        .user-info strong {
            color: #2c5aa0;
        }
        .questions-count {
            background: #f39c12;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: bold;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 11px;
        }
        .page-break {
            page-break-before: always;
        }
        .no-page-break {
            page-break-inside: avoid;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>📊 Rapport des Quiz Mentaux</h1>
        <p>Généré le {{ dateExport|date('d/m/Y à H:i') }}</p>
    </div>

    {% for quiz in quizzes %}
    {% if loop.index0 > 0 and loop.index0 % 3 == 0 %}
        <div class="page-break"></div>
    {% endif %}
    
    <div class="quiz-item no-page-break">
        <div class="quiz-header">
            <div class="quiz-title">{{ quiz.titre }}</div>
            <div class="quiz-meta">
                <span>ID: {{ quiz.id }}</span>
                <span>{{ quiz.dateQuiz|date('d/m/Y') }}</span>
            </div>
        </div>
        
        <div class="quiz-info">
            <div class="user-info">
                <strong>Utilisateur:</strong> {{ quiz.utilisateur ? quiz.utilisateur.nomComplet : 'Non assigné' }}
            </div>
            
            <div>
                <span>Niveau de stress cible:</span> 
                <span>{{ quiz.niveauStressCible }}/5</span>
            </div>
            
            <div>
                <span>Score résultat:</span> 
                <span>{{ quiz.scoreResultat }}/100</span>
            </div>
            
            <div>
                <span>Statut:</span> 
                <span>{{ quiz.statut|capitalize }}</span>
            </div>
            
            {% if quiz.medailleQuiz %}
            <div>
                <span>Médaille:</span> 
                <span>🏆 {{ quiz.medailleQuiz }}</span>
            </div>
            {% endif %}
            
            <div class="questions-count">
                {{ quiz.nbQuestions }} question(s)
            </div>
        </div>
    </div>
    {% else %}
    <div style="text-align: center; padding: 50px; color: #666;">
        <h3>Aucun quiz trouvé</h3>
        <p>Il n'y a aucun quiz mental à exporter.</p>
    </div>
    {% endfor %}

    <div class="footer">
        <p><strong>BioSync</strong> - Plateforme de santé et bien-être mental</p>
        <p>Document généré automatiquement - Ne pas modifier</p>
    </div>
</body>
</html>
